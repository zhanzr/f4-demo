#ifndef _SYS_H
#define _SYS_H

/* sys.h shim for the vendored ALIENTEK touch/eeprom code: provides the
 * ALIENTEK typedefs (u8/u16/u32/vu8/vu16...), the bit-band GPIO aliases
 * (Pxout(n)/Pxin(n)), and the delay declarations on top of this repo's HAL. */

#include "sys_compat.h"

#endif /* _SYS_H */